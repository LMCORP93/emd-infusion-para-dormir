---
import config from '@/data/site.config';
---
<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="robots" content="noindex, follow" />
    <link rel="canonical" href={config.domain} />
    <title>Página no encontrada | {config.siteName}</title>
    <style>
      body { margin: 0; font-family: Arial, sans-serif; background: #f7f2e8; color: #171717; }
      main { min-height: 100vh; display: grid; place-items: center; padding: 2rem; text-align: center; }
      .box { max-width: 38rem; background: #fff; border: 1px solid #e7dfd0; border-radius: 1.5rem; padding: 2.5rem; box-shadow: 0 1rem 3rem rgba(0,0,0,.08); }
      h1 { margin: 0 0 1rem; font-size: clamp(2rem, 5vw, 3.5rem); }
      p { line-height: 1.6; color: #555; }
      a { display: inline-flex; margin-top: 1rem; padding: .9rem 1.2rem; border-radius: 999px; background: #ff7a1a; color: #fff; text-decoration: none; font-weight: 700; }
    </style>
  </head>
  <body>
    <main>
      <div class="box">
        <h1>Página no encontrada</h1>
        <p>Esta URL no existe o contiene un error. Vuelve a la comparativa de infusiones para dormir.</p>
        <a href="/">Volver al ranking</a>
      </div>
    </main>
  </body>
</html>
